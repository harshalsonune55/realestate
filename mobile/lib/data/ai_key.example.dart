/// Template for `ai_key.dart`, which is deliberately not committed.
///
/// Copy this file to `ai_key.dart` and paste the Groq key in. The real file is
/// git-ignored so the key never reaches the repository — a key pushed to
/// GitHub is a key that gets scraped and revoked, usually within minutes.
///
///     cp lib/data/ai_key.example.dart lib/data/ai_key.dart
///
/// A build with the placeholder still compiles; the assistant simply reports
/// that it has no key.
library;

const kAiApiKey = '';
